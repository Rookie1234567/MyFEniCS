# Task002 Hybrid hard quarantine

M2C selected Review V3 option B2. The status is:

```text
hybrid_route_status = deferred_known_near_degenerate_bug
```

All Hybrid IDs have been removed from the Task002 production model registry.
The production parameter schema rejects them, the production-to-Task001
conversion fails closed, and the campaign Hybrid command raises an explicit
hard-quarantine error. Historical Case112–114 diagnostic gates and artifacts
remain readable through a separate historical identity type and were not
rewritten.

The historical Task002 Hybrid p6 identity is corrected by addendum to:

```text
uniform N1curl p6
```

Earlier wording implying p5 trace/p6 interior was incorrect. This metadata
correction does not alter raw numerical evidence.

The independent dense Floquet check uses a complex off-diagonal 6×6 matrix and
an independently constructed dense 6×4 constraint matrix. Against NumPy
`C.conj().T @ A @ C`, both MPI1 and MPI2 PETSc reductions have maximum absolute
error `5.5511e-17` and relative Frobenius error `2.6413e-17`. This strengthens
the algebraic authority but does not repair or re-enable Hybrid production.
