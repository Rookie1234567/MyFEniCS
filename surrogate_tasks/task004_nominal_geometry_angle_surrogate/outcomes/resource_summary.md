# Resource summary

The first new-SHA anchor used one MPI2 forward job with one thread per rank.
The watchdog recorded approximately 5.69 GB peak RSS and zero swap before the
direct-LU factorization failure. No parallel FEM jobs were launched and no
training or validation jobs followed the controlled stop.
