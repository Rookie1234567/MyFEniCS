# Case125：Task004 M4A/M4B/M4C training qualification

This case contains the lightweight checker and reviewed summaries for the
immutable Task004 Ny4/p5 angle-training package.  The response-bearing arrays
and raw formal records remain ignored artifacts under
`benchmarks/artifacts/cases/125_task004_angle_training_qualification/`.

The checker is response-blind to Task003 validation and does not execute FEM.
It independently reads the Case124 campaign manifest and formal records,
reconstructs the 96-row coverage, and recomputes package hashes and array
identities.
