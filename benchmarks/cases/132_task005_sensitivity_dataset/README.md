# Case132 — Task005 16-angle sensitivity dataset

Case132 independently rebuilds the central finite differences from the raw
aggregate and fixed-order power arrays.  It verifies the exact 16-angle tuple
table, source/config identity, the 20 M1 record reuses, and the 44 genuinely
new M2 states.  It never opens Task004 blind validation and never runs a
solver.
