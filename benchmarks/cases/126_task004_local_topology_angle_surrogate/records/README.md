# Case126 records

`case126_check.json` is the only tracked record produced by this case.  It is
an independent audit of the immutable train96 package and M4E response-blind
qualification artifacts.  There are intentionally no FEM run directories,
blind-validation responses, or active-learning result records here.
