# 合并建议

建议合并：Task025 文档、轻量 CSV/JSON、通用 complex p-transfer 修复、p1 AMS degree 修复、Task23 shifted FE sub-operator 支持，以及 opt-in Task025 research runner。

不建议合并到 production 默认：cached-Schur profile、h=2 参数、未通过 gate 的 R/T/A 路径、adaptive BDDC 和定制 2D coarse。

`results/`、papers、本地 mesh/matrix/Q cache 不提交。
