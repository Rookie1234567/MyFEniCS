# 运行日志

所有运行均在 Docker `code-dolfinx-mpc:latest` 内执行，并先启用 `. dolfinx-complex-mode`。

## 原 task006 关键事件

- p=2 assemble-only h=1.5 在 7200 s timeout；h=1 在 base matrix assembled 后被 signal 9 kill。
- 初次 direct p=1 h=5 暴露 reduced-height domain 中默认 top probe 落入光栅内部的问题，随后修正 `_probe_z_locations` 并重跑 direct。
- p=1 default direct 完成到 h=2，h=1.5 在 `stage4_dtn_augmented_ksp_setup` 被 signal 9 kill。
- p=2 default direct 完成到 h=4，h=3 在 `stage4_dtn_augmented_ksp_setup` 被 signal 9 kill。
- memory profiling 运行 p=2 h=5 MPI=8 default direct，输出 `memory_profile_summary.csv` 和 `memory_profile_timeseries.csv`。

## 补充 OOC tuned 运行

补充前修正了 PETSc extra options 覆盖顺序，确保 `mat_mumps_icntl_14=200` 能覆盖 `mumps_ooc` profile 默认值。

- p=2 h=5 tuned OOC：completed，elapsed 140.7 s，OOC scratch 约 4.95 GB。
- p=1 h=1.5 tuned OOC：failed，PETSc error 76，MUMPS `INFOG(1)=-90`。
- p=2 h=4 tuned OOC：completed，elapsed 1180.4 s，OOC scratch 约 14.24 GB。
- p=2 h=3 tuned OOC：failed，PETSc error 76，MUMPS `INFOG(1)=-90`。
