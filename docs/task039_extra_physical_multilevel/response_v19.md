# Review V18 执行回复：Response V19

当前进展（原始模型完成后的增量）：U4 第三次运行已独立核验通过，564 步、最终 A6=9.92314718715201e-7，L2/scaled-curl=1.3644783292666524e-8/4.3714257232960455e-9。R/T/A/A_volume=0.3656257909689885/0.012990632321222292/0.6213835767097892/0.6213835745968793；完整物理 Gate、80模式、近场与守恒通过。全过程 RSS峰值2528460800 B，库存1830284886 B，临时396129600 B，zero-swap与清场通过。该场 monotonic 6609.661379 s、保守结算7210.314085 s；本批累计保守7709.181734 s。源码8a2d5cbba6ed834a6d731a30dd3735c8824fa762；28项只读checker/ledger测试通过。U5同配置准确notch已准入，尚未运行。下方两次失败及其当时停止结论属于历史快照，全部费用保留，不再是当前阻断。

用户最新授权：完整地跑个p6h10的模型，看看结果，如果不是数值gate，那么就修正那些bug。以下两场实现失败及费用保留为续跑前快照；原重放额度不再阻断本次有明确 bug 证据的续跑。准确凝聚、原数值/物理/资源 Gate 和 observe_only 均保持，U4 正在继续，U5 仍以 original 完整通过为条件。

| 审阅问题 | 本轮结论 |
|---|---|
| 准确单元凝聚是否等价且省内存？ | **规定 p4 检验通过；全过程 RSS 从 2,825,973,760 B 降为 1,785,585,664 B，减少 36.8152%。** 三 RHS、重复与线性检查、非零内部 RHS 恢复和严格 slave-zero 均通过。 |
| 新矩阵 BLR 是否有额外价值？ | **没有。** 唯一 tau=1e-5 的质量通过，但实际条目仅减少 2.3564%，RSS 增加 1.4163%；选择准确凝聚。 |
| 完整 original/notch 是否通过？ | **未通过/未运行。** original 两次启动分别遭遇计时参数检查和返回记录错误；本批唯一 bug replay 已用完。第二次完成了一个真实 BAL_H PC，但返回被异常丢弃，尚无有效外层更新。此结果不能解释为数值方法不收敛。 |

本轮未停在 builder 或内部 PASS：已实际进入完整 original 的 KSP 和在线 BAL_H/H6。此前按原执行次数上限暂停，最新用户授权后继续 U4；不把局部等价、一个 PC 或修复后的单元测试提升为完整三维资格。本次将记录有证据的第三场 PDE，不改变 epsilon 或算法路线。

单元凝聚把只属于单个单元的未知量先在单元内消去，再解共享边界和原 80 端口，最后恢复全部内部场。它改变全局分解的规模，付出局部 LU、右端项缩减、恢复和缓存的费用。本轮每个完整单元的 curl 与复材料项先相加，再形成 Schur；未先装配全局 p4，也没有 42 宏块或 42/252 份内部 MUMPS。252 个单元对应 5 类 raw 张量、12 类定向局部 LU；独立 trace 21,744 加 80 端口，全局为 21,824 行。每次非零输入只有一次全局 MatSolve，再恢复 27,216 个内部坐标，返回完整 53,084 坐标的 p4 slave-zero 向量。

缩减包含 `g_t−V_ti V_ii⁻¹ g_i` 和 `D_i V_ii⁻¹ g_i`；端口包含 `Bhat、Dhat、H+D_i V_ii⁻¹ B_i`，恢复为 `V_ii⁻¹(g_i−V_it c_t−B_i alpha)`。收到的输入已是 MPC dual storage，不重复施加 Cᴴ。当前材料/几何边界为已资格化轴对齐仿射 hex 与 cell-wise 材料；不宣称任意曲面通用支持。

| 冻结输入 | exact 原 A4 残差 | exact L2 | exact scaled-curl | BLR 原 A4 残差 | BLR L2 | BLR scaled-curl |
|---|---:|---:|---:|---:|---:|---:|
| A2R160_BAL_H_p4_01 | 7.52866012e-11 | 5.20206277e-12 | 5.19947161e-12 | 0.00762754585 | 0.000156967577 | 0.000153761427 |
| A2R160_BAL_H_p4_02 | 3.70778404e-12 | 5.21266719e-12 | 5.21031519e-12 | 0.000482348706 | 0.000248997417 | 0.000244243916 |
| LIGHT448_BAL_H_p4_09 | 6.72895057e-11 | 5.21310781e-12 | 5.21106919e-12 | 0.0110374087 | 0.000184090086 | 0.000180450982 |

exact 的 A4 限值为 1e-10、L2/curl 为 1e-8；BLR 局部筛选为 0.5/0.25。三组 exact 残差恒等式操作尺度检查通过 1e-10，所有返回的 slave 非零数为 0。额外重复01、01+i02、09−01的 A4 残差为 7.52866e-11、2.48054e-11、4.61608e-11；线性偏差为 0、1.83755e-13、1.76400e-13。全局 symbolic/numeric 始终各1次，累计6次 MatSolve，局部 LU 数目与身份不增长。

CSR 按实际 owned 行/列/complex128 内容流式计算，exact 与 BLR 同为 `19b9fbf759e6dc586d1316b53c69099647bd3a23e43378535b718c1db2c218d8`，8,184,464 个存储条目；factor 前后及 control 调用后相同。旧 Q1 缺 CSR hash 如实保留，不为此重跑。Q1 的同环境、同三输入、公共对象及因子保留轨迹可复用；本批未新增 matched full-p4 control。

内存单位均为 B；GB=10⁹ B，GiB=2³⁰ B。RSS/PSS 是连续同时进程树实测；allocated/used 是 MUMPS 各自原生整数 MB 加保守舍入，不能相互替代。

| p4 control / 历史对照 | 全过程 RSS | factor-live RSS | 保守库存峰值 | MUMPS allocated upper | MUMPS used upper |
|---|---:|---:|---:|---:|---:|
| Q1 原 full-p4 LU | 2825973760 | 2825973760 | 2906619390 | 2343000000 | 1382000000 |
| 旧 Q2 宏块 Schur，历史负对照 | 4267347968 | 见旧记录 | 4698023554 | 见旧记录 | 见旧记录 |
| U2 cell exact | 1785585664 | 1785585664 | 1776346158 | 1463000000 | 838000000 |
| U3 cell BLR | 1810874368 | 1810874368 | 1340346158 | 1027000000 | 874000000 |

U2 的已预分配矩阵账为 232,205,060 B，局部 LU/恢复/RHS 缓存合计 11,327,040 B（其中 LU 2,244,672 B），连同映射、公共对象和全局因子全部纳入库存。临时预留峰值 134,217,728 B；追加三次调用后的全 run RSS 仍为 1,785,585,664 B。不保留 raw/Schur 同义副本，不做 heap-trim 或提前释放因子优化。所有正式运行的整机 reserve、8 GiB tree cap、6 GiB 库存、1 GiB 临时池、zero-job/new-global-swap 和最终进程清场均通过独立轨迹检查。

U3 实际 ZMUMPS 输出为 5.6.2；35=2 与 tau 在 symbolic 前设置并读回，10=0，每次 solve 的 36/37/38=0/0/600。34 个 BLR fronts 覆盖85.5%，原生本矩阵条目40,282,272→39,333,043。初版 checker 错把初始化前默认 ICNTL38=0 当作运行值；改为检查每次实际 solve 的600，同时保留35/tau的symbolic前检查。初版负检查结果保留，仅重新读取证据，未重跑计算。

完整 original 首次在创建 KSP 前被旧 `solve_limit==10800` 检查拒绝；修复后唯一重放在第一个 PC 返回时遇到 `0` 秒计时观察值无效，随后“PC 尚未结束”的清理错误掩盖原因。两份原始异常、失败 packet 和费用均保留。第二次 PC 的两次在线 p4 残差为7.30181e-11和3.28874e-12，BAL_H闭合为6.84671e-12≤1e-8；这支持接线的小量一致性，不能推断外层收敛。

最新停止后修复 `14f0bdf6627c98c41cac0b6f9784e07a415cd56d` 只把新profile的观察参考值设为正数25/30秒（仍强制observe_only，不作为停止线），并先解除已返回PC的计时标记，再验证记录，以保留原始错误。最终定向测试99通过；**该修复后没有新正式PDE**。原A6最后保存值为第0步的1，最终A6不存在；无official E/H、近场、R/T/A、A_volume、80模式或独立守恒结果。原1e-6和完整物理Gate未放宽。restart32/max2048已实现并经小型真实KSP测试，完整模型尚无restart效果或总收敛成本证据。

| 正式场 / attempt | 全流程 monotonic s | 保守结算 s | 全程 RSS峰值 B | 真实终态 |
|---|---:|---:|---:|---|
| U2_EXACT_CONTROL / 1 | 93.132393 | 103.412738 | 1785585664 | worker_exit0 |
| U3_BLR_CONTROL / 1 | 89.987755 | 98.824883 | 1810874368 | worker_exit0 |
| U4_ORIGINAL / 1 | 147.527291 | 162.415457 | 2504851456 | WORKER_FAILED |
| U4_ORIGINAL / 2 | 123.891613 | 134.214571 | 1878298624 | WORKER_FAILED |

正式 monotonic 合计 **454.539053 s**，独立保守账合计 **498.867649 s**，包括两场失败和一次重放；4次全局symbolic/numeric、11次MatSolve，无活动预留。U2 setup为32.9821 s，numeric21.5539 s；U3 setup33.8348 s。U2三次纯缩减/回代/恢复为0.23449/0.21033/0.18422 s，native评价、metric和保存时间在compact逐项列出。Q1 numeric18.4888 s，减内存并未让numeric更快；旧Q1总536.4680 s含历史JIT/缓存/时钟成本，不直接与本次93.1324 s相除宣称算法加速。U4两个峰值均只是失败前缀，不能称完整p6峰值。

新ledger引用旧V14/V16/V17，不清零历史费用；旧600秒只保留为政策占用，实际耗时仍unknown。可界定的正式运行前工程观察窗口为7126.093 UTC秒，包含root/Luna重叠的阅读、开发、测试、等待，不是CPU时间；更早准备和独立拆分的后续修复/文档耗时未测量，不填0。没有用费用重算重新开放试验次数。

| 阶段 | 统一状态 |
|---|---|
| U0/U1 | 身份/小型数学/接线/清理资格通过，复用Q1与既有参考；旧41份冻结文件和30项run前缀保留 |
| U2 | 准确p4等价与资源通过，允许作为默认准确候选 |
| U3 | 唯一凝聚BLR质量通过但额外内存收益失败，选择exact |
| U4 | 实际执行两次，implementation blocker；修复经小测试，重放额度已耗尽 |
| U5 | original未完整通过，条件不成立，未运行 |
| U6 | 统一保存并提交待审，不合并master |

正式源依次为 `e1b5a398a199cdbc7c26ce3af8645daed2eeb7d6`、`5e95066d80579b0e135643f9fd114bd3dcc43424`、`4f15f7182289b3911bba6f766c5cd7e9a7026401`、`37e25d63fd5ac56ad95425a292ad291a7e564d56`；base为`96827e89ce5ba446c4faa7cb507b777f21953af8`。全部在canonical worktree的`task39extra`，不修改5nm线。最终交付HEAD由Git与推送回执给出，避免文档自引用SHA。

证据入口：[统一结果](outcomes/p4_cell_condensed_v18.md)、[compact](outcomes/records/p4_cell_condensed_v18_compact.json)、[decision](outcomes/records/p4_cell_condensed_v18_decision.json)、[run index](outcomes/records/run_index.json)、[测试](outcomes/test_summary.md)、[合并边界](outcomes/selective_merge_manifest_v19.md)。大型向量与连续轨迹保留在ignored结果目录，Git提交可审阅的数值、身份和hash索引。未安装Ruff；未运行full repository pytest或声称CI通过。

已取得用户新的准确original运行授权，完整通过后再同配置notch；不建议换PC、调tau或扩大步数/rank。本轮不宣称已取得有效完整PC或完整三维优化成功。
