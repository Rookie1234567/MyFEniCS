# Selective merge manifest V25

本 manifest 覆盖 Review V23 S0–S6 的 base 到 final 差异，而不是只列 S6 文档。review base 为 e63d2f49f19961f985faeadbe1ad3b89808369d1，formal final source 为 cad282e25ed53cad1f9e4a5a70c14f3dd40e6d32；中间包含 a80ecab 的实现、b79cff2 的组件冻结和 cad282e2 的 public launcher 入口修正。总计 38 个文件、5244 行新增、223 行删除。S1/S2 实现完成后 source 冻结；Q4/Q3/Q2 运行和 S6 没有继续修改数值实现。

sum-factorized 后端把 A6/H6 的局部重复动作变成可复用、有计数、有 timing 的内核，收益是减少 operator 动作成本并改善审计；代价是新增 solver、runner/schema、输入参数和测试。粗阶 formal 场则回答 p4/p3/p2 预条件路径的离散残差、物理 Gate 和资源边界。operator pair 的加速不能直接当成 full-PC 或 end-to-end PDE 加速。

| 依赖组 | 本批内容与文件边界 | 数值行为/证据 | 对应测试或 fresh evidence | 建议 |
|---|---|---|---|---|
| production numerical/core | src/solvers/fullspace_n1e_sum_factor.py；fullspace_partial_assembly.py；physical_equivalent_fast.py；physical_light_setup.py；fullspace_mpc_action.py；fullspace_quadrature_diagonal.py；physical_retained_fgmres.py；相关 retained/cell-condensed runner | S1/S2 实现了 A6/H6 后端、P6q 粗阶参数和 retained timing；行为已由 Q4/Q3/Q2 source cad282... 冻结并绑定 | S1 operator equivalence；Q4/Q3 formal residual/physical Gate；Q2 controlled-stop negative evidence | ChatGPT review 后按依赖顺序合并；不把 Q2 变成 production pass |
| reusable runner/watchdog | scripts/run_case.py；src/runners/task038_full3d_iterative.py；task038_launcher.py；physical_p4_schur_v14.py；physical_retained_outer_adapter.py；input schema/validation/profile 文件 | 增加 source/input/config binding、coarse-degree routing、lifecycle/resource ledger 和 terminal classification；Q4/Q3/Q2 均由同一 schema 记录 | v25 q2/q3/q4 raw run roots、watchdog summary/resources、common freeze manifest | 与 core 一起审阅；不要拆出不带 schema/source identity 的局部 runner |
| checker/benchmark | benchmarks/task39extra_v25_dynamic_checker.py；benchmarks/fe_metric_v25_q4_glue.py；src/test/test_task39extra_v25_dynamic_checker.py；src/test/test_task39extra_v25_first_direction_pair.py；src/test/test_task39extra_v25_parameterization.py；相关旧 tests 更新 | checker 从原始字段重算，不创建 solver；FE glue 只读取既有 packets、无 PDE；Q4/Q3 DYNAMIC_PASS，Q2 terminal checker not_run | focused checker/parameterization tests；Q4/Q3 audit entry 与 official output；promoted glue py_compile | 可在 core/runner 审阅通过后合并；保留旧 checker 和负结果 |
| input and compact evidence | input/task39extra/v25_q2_speed_h7p5.dat、v25_q3_speed_h7p5.dat、v25_q4_speed_h7p5.dat、input/README.md；S1 compact；v25 common freeze manifest | 输入 hash、physical/mode/source identity 固定；S1 selected v27 operator pair、旧 v26 H6 负候选均保留 | source/input/config hashes、S1 paired resources 和 equivalence fields | 与 compact docs 一起合并；不是新算法默认配置 |
| compact evidence/docs | response_v26.md；outcomes/a6_h6_coarse_degree_v25.md；outcomes/records/a6_h6_coarse_degree_v25_*.json；summary.md、test_summary.md、development_progress.md、development_model_registry.md、run_index.json | 只登记可复核事实；Q2 的 unknown/partial/not_run 不升级；V24 112 步 p4 baseline 与 Q4 112 步 raw monitor record 单列 | JSON parse、git diff check、raw artifact hash index | 优先合并；保留所有历史 v24 及更早证据 |
| research-only | S1 v26 operator 负候选；Q2 RESOURCE_CONTROLLED_STOP；ignored fe_metric_v25_q4_glue.py 及其旧 audit tool SHA | 研究/负结果和历史 artifact 工具身份，不是 production default；Q2 无 official output | S1 compact、Q2 watchdog/events/iterations/workflow_phase、Q4/Q3 audit raw entries | 只作为证据保留；tracked glue 不改变 ordinary default |
| do-not-merge | ignored raw result trees、Q2 partial field/active PC、未完成 final KSP/official output、任何把旧失败改成 PASS 的文件 | 不改变数值行为；禁止冒充 official result 或 continuum convergence | 无 | 不作为 production 或 official evidence 合并 |

建议顺序：production numerical/core → reusable runner/watchdog → checker/benchmark → input/compact evidence → response/outcomes/index → selective manifest。正式合并仍需 ChatGPT 明确批准并由用户授权；ordinary default 和 master 状态保持不变。

FE glue promotion：tracked benchmarks/fe_metric_v25_q4_glue.py 只把既有 ignored wrapper 变成可审阅文件，并把 artifact-root 的 REPO_ROOT parents[5] 修正为 repository benchmarks 文件下的 parents[1]。old ignored SHA c52dde9f0e02390d3c8ba8d0fe67348eb4149d938ebe0a41da742d7a47156648、audit-recorded SHA 6b267291c69f9445441b39875a6c1c324db796ede0acf2e3e9e6e3f8140cc5a5 和 promoted SHA 507fe2b7de074dc5482b4f8343e6920a23618c11d05efb1a41ad6c86c5896099 均保留；promotion 后不重算 FE。
