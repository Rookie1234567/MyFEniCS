# Task39extra Selective Merge Manifest V21：Review V20 双层凝聚生命周期

本 manifest 按依赖组说明 V20 的合入边界。它不是 master merge approval；当前只完成一场固定 original 的 fresh evidence，ordinary default 保持不变。

| 依赖组 | 文件/范围 | 数值行为 | 依赖与测试 | fresh PDE evidence | 建议 |
|---|---|---|---|---|---|
| production numerical/core | `src/runners/physical_dual_cell_condensed_lowmem_v20.py`、`physical_retained_outer_adapter.py`、`physical_p4_schur_v14.py`、`physical_p4_cell_condensed_v18.py`、`src/solvers/hcurl_assembly_time_condensation.py`、`src/solvers/physical_interface_balanced.py`、`src/solvers/fullspace_same_mesh_hcurl_pmg_physical.py`及 `src/postprocessing/{postprocess_3d,rta_3d,diffraction_3d}.py` hooks | 改变 opt-in V20 的 form preparation、identity representation、factor/field release lifecycle 和 official postprocess ordering；p4 matrix 保留，不改变 V18/V19 default | Y1 `116 passed / 1 skipped`、compileall、frozen contract；依赖 qualified PETSc/MUMPS v3.19.6、DOLFINx/Basix ABI；核心与 hooks 是同一依赖组 | Y3 original official pass，A6 independent `9.730817853580463e-7`，full RSS `2831749120 B` | 只能作为显式 opt-in research candidate 审查；不得直接切普通 default，先经 review |
| reusable runner/watchdog | `scripts/run_case.py`、`src/runners/{task038_launcher,task038_full3d_iterative}.py`、`src/io/{input_schema,input_validation,physical_intermediate_profile}.py` 和 `input/task39extra/v20_y3_lowmem_original.dat` | 增加 V20 输入路由和连续资源/事件证据绑定；不改变旧 profile 语义 | Y1 input admission、service/ABI/clean-source gates、Y3 run summary | one-dat/one-run service exit0、zero swap、descendants cleared | 与 core/hooks/profile 按依赖组审查；输入仍须显式 profile |
| checker/benchmark | `benchmarks/check_dual_condensed_memory_v20.py`、`src/test/test_task39extra_v20_checker.py`、`src/test/test_task39extra_v20_y1_lifecycle.py`、`src/test/test_task39extra_v20_y3_mock.py` | 只重算和检查记录，不实现新的 PDE 数值核心 | Y1 focused、independent 57 checks/24 physical subchecks、raw hash-bound lifecycle/phase evidence；checker/test 依赖 core 的新 event/schema/hooks | checker 与正式 run 一致通过 | 与 production core/hooks 作为依赖组审查，不建议单独合入；不可把 checker 通过当作 production qualification |
| compact evidence/docs | `response_v21.md`、`outcomes/dual_condensed_memory_v20.md`、V20 compact/decision、`outcomes/summary.md`、`test_summary.md`、`run_index.json`、`docs/development_progress.md`、`docs/development_model_registry.md`、本 manifest | 不改变数值；记录 mixed tradeoff、unknown、not_run 和历史负结果 | JSON/Markdown/link 检查应在最终文档后运行；证据绑定 b337… 与 Y3 artifact hashes | V20 evidence complete for fixed original only | 可审阅后合入 docs/evidence；不覆盖 V18/V19 历史 |
| research-only | generated V20 cache/module/raw timeline、Y3 full arrays/fields/matrix/factor、root_engineering full logs and ignored result root | 保留用于审计，不提升为 production input/default | 依赖 ignored artifact path、hash and source identity | full raw evidence exists but is not a compact tracked artifact | 不进入 Git/master；需要时按 hash 重新定位 |
| do-not-merge | 任何未经 review 的 allocator/unsafe matrix-free experiment、未资格化的新算法默认、generated C 差异诊断推断、ignored heavy artifacts | 不得把未知/负结果改写为通过、零费用或矩阵可安全释放；不得提升 research path 为 production default | V18/V19 的 old failures、policy/unknown accounting 和 notch interrupted records 必须保留在历史 compact/docs，不得删除或迁移成新通过项；本轮无新 PDE 授权 | notch/5nm/0.7nm/MPI2/4 not_run | 不进入 production/master；历史证据继续保留，待后续明确 review 才能扩展 |

## 结论与顺序

V20 的数值和物理结果可以作为固定 original 的独立证据；其全过程 RSS 比 V19 低 `28.59098%`，但比 V18 高 `11.99498%`，且比 V19 measured monotonic 时间高 `9.4056%`。采用建议是：在同一 original 的内存优先场景使用显式 lowmem profile，继续把 V19 作为时间基线，普通 default 不变；不新增“必须低于 V18 峰值”或“必须快于 V19”的合入门槛，也不重启 detach 研究。审查顺序为：先审 compact/checker/docs，再按依赖组审 opt-in core/hooks；不以本场一次结果宣称 0.7 nm、任意非可分单胞或 production scalability。
