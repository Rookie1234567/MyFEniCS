# Selective merge manifest V29：显式 profile 待审

本清单按 Review V27 的依赖组区分可审查实现、支撑文件、证据和禁止迁移项。源基线为 `780f58918b0e5a9868cd2ea3de26d451bc6b5d86`；P1/P2/P3 已在显式 V29 profile 中测量，但仍待主控 review，不代表普通默认获批。本清单不是 merge approval；合并仍须主控明确批准及用户授权。旧版 manifest 保留不改。

| 依赖组 | 文件与依赖 | 数值行为、测试与 fresh PDE 证据 | 建议顺序 |
|---|---|---|---|
| production numerical/core | `src/solvers/physical_interface_balanced.py`、`physical_equivalent_fast.py`、`fullspace_n1e_sum_factor.py`、`fullspace_partial_assembly.py`、`hcurl_assembly_time_condensation.py`、`physical_dual_cell_condensed_lowmem_v20.py`、`task39extra_p6_raw_tensor.py`、`physical_light_setup.py`、`src/runners/physical_p4_schur_v14.py`、`input/task39extra/v29_a4_tensor_h6_original_h7p5.dat`。 | P1 改变“粗层目标未达但状态有限”时的处理：保留最佳完整状态并继续外层；P2/P3 保持方程目标，改变完整 A4 与 p6 局部张量构建实现，误差门限通过。focused source suite **79 passed, 1 skipped**（`test_362_partial_assembly.py`、`test_task39extra_v24_p4_repair.py`、`test_task39extra_v25_dynamic_checker.py`、`test_task39extra_v28_profile.py`、`test_task39extra_v29_p6_raw_tensor.py`；唯一 skip 是 tiny MPI2 qualification）。1次 V29 integrated fresh PDE 通过本离散残差/物理 Gate；P1软目标未达继续为`0/262`，故此分支只有 fixture 覆盖；P2完整 A4 共267次；P3的12类均通过。证据仅支持 explicit V29 profile，不支持普通默认或跨模型推广。 | 主控先审该数值组及其 profile 依赖；获批后仍保持显式启用，不自动改默认。 |
| reusable runner/watchdog | `scripts/run_case.py`、`src/io/input_schema.py`、`src/io/input_validation.py`、`src/io/physical_intermediate_profile.py`、`src/runners/physical_retained_outer_adapter.py`、`src/runners/task038_full3d_iterative.py`、`src/runners/task038_launcher.py`。 | 负责配置校验、显式 profile 选择、worker/launcher 生命周期与 watchdog 接线；不单独改变方程，但 profile dispatch 会选择上组数值路径。由同一79/1 focused suite覆盖；V29 一次 formal run 实际走通，watchdog exit 0、后代清理。 | 与 production core 同批审查，确认 profile 默认仍关闭；不能把 orchestration 改动误判为独立数值资格。 |
| checker/benchmark | `benchmarks/task39extra_v25_dynamic_checker.py`；对应 `src/test/test_362_partial_assembly.py`、`test_task39extra_v24_p4_repair.py`、`test_task39extra_v25_dynamic_checker.py`、`test_task39extra_v28_profile.py`、`test_task39extra_v29_p6_raw_tensor.py` 与 compact checker record。 | checker 从本场原始字段重算状态，不实现求解器；本场为`DYNAMIC_PASS`，完整显式残差`9.283162362107749e-7`并通过物理 Gate。fresh PDE证据就是上述唯一 V29 integrated run；离线 V28场/模式/功率复核是只读，不另算 PDE。 | 数值组审阅后审 checker 与记录；checker保持独立于solver，不将状态标签当唯一依据。 |
| compact evidence/docs | `response_v30.md`、`outcomes/a4_tensor_h6_v29.md`、`outcomes/records/a4_tensor_h6_v29_*.json`、`outcomes/summary.md`、`test_summary.md`、`run_index.json`、`docs/development_progress.md`、`docs/development_model_registry.md`、本 manifest。大型 field/矩阵等不在本组。 | 文档合同 **21 passed**；本轮7份 JSON 可解析，`git diff --check`通过。全部引用同一 V29 formal run、同源SHA/数据hash；文档与compact record不生成数值结果。CPU秒数按Review V27边界均明确为`unknown`，没有由 wall-clock 推算。 | 在实现与checker审阅后合入轻量证据；保留旧 failure/partial 记录和缺项分类。 |
| research-only | `benchmarks/run_task39extra_v29_a4_action_pair.py`、`benchmarks/run_task39extra_v29_p6_tensor_pair.py`、`benchmarks/run_task39extra_v29_h6_transform_pair.py`；未采用的 H6 stacked-real/imag 试验及组件级 engineering measurements。 | 三个 component runner 仅复现配对实验，不是通用生产 runner。P2 配对中位数约`2.818x`、P3 12类配对通过；未采用 H6 候选输出相同但 apply 慢`1.69%`。这些独立组件试验均无 formal PDE；本场正式运行使用的不是未采用 H6 candidate。组件小测试与成本见 outcome/components record。 | 保留用于研究复核，不迁移为 reusable runner 或生产默认；若要重新资格化需另立授权。 |
| do-not-merge / default promotion | ignored 的完整场、NPZ数组、矩阵、因子、cache、timeline、原始日志；R1未完成尝试不得作为 formal pass；将P1/P2/P3切为普通默认或纳入未审review分支。 | 大型原始对象继续留在 ignored artifact 路径；本用户已选择保留 R1 部分证据并停止重跑，缺失 BAL_H/i112 仍为缺失。没有新的 PDE 授权；不以一次 integrated V29 或组件结果替代外部审查。 | 不迁移/不改写/不提升默认。等待主控 review 与用户明确合并授权。 |

建议依赖顺序：production numerical/core → 显式 profile 与 reusable runner/watchdog → checker/benchmark → compact evidence/docs。research-only 与 do-not-merge 项不进入该序列。V29 仅是一场离散模型的 authority-limited 通过；完整 phase resource/time 边界及CPU未知项见 [V29 outcome](a4_tensor_h6_v29.md) 与 [components record](records/a4_tensor_h6_v29_components.json)。

空间/约束/端口/JIT 准备、bridge/startup checks 与外层正交化的独立 wall time 未记录，均为 `unknown`；不得从其他阶段或累计桶反推。
