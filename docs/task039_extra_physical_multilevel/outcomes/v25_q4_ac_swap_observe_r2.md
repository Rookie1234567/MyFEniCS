# V25 Q4 AC 重跑 r2 结果

这是用户因上一次断电后明确授权的同配置 Q4 original 重跑。它保持 p6/h7.5、粗阶 p4、MPI1/thread1、物理模型和求解器不变；只增加独立 `run_id` 与 `observe_only` 资源观察记录，不改变数值算法。上一次被控停止的尝试仍单独保留。

## 结论

本次服务正常退出（worker/service `0`，watchdog `COMPLETED`），在 outer iteration 126 达到完整显式真残差 `9.283165086752956e-7 < 1e-6`，post-release 残差相同。独立动态 checker 为 `DYNAMIC_PASS`，物理一致性检查、资源可读性和清场均通过；结果仍是 `DISCRETE_SOLVE_AND_CONSISTENCY_PASS_AUTHORITY_LIMITED`，因为没有匹配的 h7.5 independent reference，不能外推为 continuum convergence。

本次 `AC1` 的首次成功启动后检查和结束检查均为在线；没有观察到运行中的 AC1 断开。启动前探针因未激活资格化环境而没有读到 AC1，因此不把这次观察解释为“已证明上次唯一原因就是拔电”。没有修改电源设置。

| 指标 | r2 实测 |
|---|---:|
| final / post-release 显式真残差 | `9.283165086752956e-7 / 9.283165086752956e-7` |
| final outer iteration | 126（达到门槛，未运行 128） |
| solver elapsed（summary） | 2514.9931942840362 s |
| full workflow monotonic | 3114.283619607013 s |
| worker RSS/PSS 峰值 | 7386742784 / 7354803200 B |
| watchdog 进程树 RSS 峰值 | 7390937088 B |
| 作业树 VmSwap 峰值 | 0 B |
| WSL 全局 `pswpout` 增量 | 0 页（基线 9，结束 9；仅诊断口径） |
| 官方物理量 `R/T/A` | 0.3650975537006154 / 0.013016803347760878 / 0.6218856429516237 |
| `R00_s/R00_p/R00_total` | 0.3650608628870385 / 3.4352368280737534e-25 / 0.3650608628870385 |

## 每 16 步与前一个完整 Q4 对照

以下耗时用两次运行相同的 residual-checkpoint `solve_seconds` 字段；残差在对应记录上完全一致。正数“节省”表示 r2 更快。

| iteration | r2 显式真残差 | r2 耗时（s） | 前一完整 Q4 耗时（s） | 节省（s） |
|---:|---:|---:|---:|---:|
| 16 | 0.0041437222964080065 | 323.3362546709826 | 356.4489566070092 | 33.11270193602661 |
| 32 | 0.0003352917960387092 | 635.7431107280124 | 699.1251847339931 | 63.38207400598071 |
| 48 | 0.00019419221371048617 | 957.0546633300296 | 1059.2216401279873 | 102.16697679795766 |
| 64 | 3.0233523443058283e-5 | 1270.2234611949964 | 1399.241939390997 | 129.01847819600061 |
| 80 | 1.760946862124978e-5 | 1590.69534846704 | 1914.236668254 | 323.54131978696 |
| 96 | 3.7766998795463687e-6 | 1903.8770562190532 | 2676.8190681720002 | 772.942011952947 |
| 112 | 2.71399585136905e-6 | 2225.8895136200404 | 3437.2333360950015 | 1211.3438224749611 |

和刚刚被控停止的 AC repeat 在 iteration 16 直接比较，r2 残差同为 `0.0041437222964080065`，耗时 `323.3362546709826 / 356.40948556199953 s`，r2 少 `33.07323089101692 s`。

## 112 步对应 p4 baseline

原始 p4 记录确实有每次调用的 elapsed；没有独立的 p4 内存字段，故内存不填。这里的 elapsed 是 raw record 中 `interface_facts.elapsed_seconds`，不是整个 outer solve 时间。

| iteration | logical call | r2 p4 残差 | r2 p4 elapsed（s） | 前一完整 Q4 残差 | 前一完整 Q4 elapsed（s） | 内存 |
|---:|---:|---:|---:|---:|---:|---|
| 112 | 1 | 3.578488052253746e-11 | 0.7877780729904771 | 3.578488052253746e-11 | 1.7836893460043939 | 无独立记录 |
| 112 | 2 | 3.470567778839567e-13 | 0.7901757570216432 | 3.470567778839567e-13 | 1.907731957995565 | 无独立记录 |

iteration 16 的 p4 call 1/2 也分别为 `9.12762008915742e-12 / 6.997011824771178e-13`；r2 elapsed 为 `0.7844888400286436 / 0.8296190820110496 s`，前一完整 Q4 为 `0.8465250529989135 / 0.8734569280059077 s`，同样没有独立 p4 内存字段。

## 证据入口

- [compact result](records/v25_q4_ac_swap_observe_r2_result.json)
- [run index](records/run_index.json)
- 原始 run root：`results/euv_grazing1_phi0/task39extra_v25_q4_ac_swap_observe_r2_h7p5__full3d_iterative__mpi1__Mna/20260922T033647.910623Z`
- 原始物理 summary：`.../physical_dual_condensed_coarse_degree_v25_summary.json`
- watchdog：`.../watchdog/summary.json`
- dynamic checker：`.../v25q4_dynamic_checker.json`
- AC 观察：`.../power_observation_start.json`、`.../power_observation_end.json`

所有旧 Q4 通过结果和上一次受控停止记录均保持不变；本次没有启动 Q3/Q2/notch 或其他额外计算。
