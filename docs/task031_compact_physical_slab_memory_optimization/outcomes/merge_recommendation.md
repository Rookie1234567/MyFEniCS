# Task031 合并建议

## 建议经 review 后合并

- external simultaneous RSS/cgroup/swap/stage sampler 与 clean-source/watchdog Gate；
- public MPC form action adapter 及 slave unit-row correctness；
- condensed operator 的 external fine action、`require_f/release_f` 与 no-double-destroy lifecycle；
- PC linearity/determinism certification、对象 ledger、true-residual monitoring；
- physical-slab fixed/selective research knobs（只保留显式 opt-in，不进入普通默认）；
- Case070 轻量 records、Task031 outcomes、测试、solver/theory/walkthrough 文档。

## 不得提升

- 不把 Task031 candidate 设为 ordinary default；
- 不导出 fixed Richardson 为成功 solver；
- 不提升 boundary Jacobi、restart50、20-slab 失败配置；
- 不实现/宣称 factor sharing；16 个 factor 无 exact duplicate；
- 不把 allocator-retained current RSS 下降写成 peak success；
- 不宣称任意参数的数学收敛保证或 mesh-independent。

## 决策

`strong_memory_success_slow_but_memory_efficient` 支持选择性合并上述通用能力与显式 profile；是否进入 master 仍需 Task031 review 通过和用户明确许可。
